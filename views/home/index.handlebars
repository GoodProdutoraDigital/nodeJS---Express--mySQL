<div class="container" align="center">
    <div> 
        <div class="jumbotron">
        <h1 class="display-3 mt-5">Bem vindo ao Admin Cpainel!</h1>
            <p class="lead">Porque não adicionar um tamplate AdminLTE ? Fique avontade !</p>
        <hr class="my-3">
            <p>Para este simples Admin Cpainel utilizamos o banco de dados mySQL e sistemas de Autenticação, Validação, Sessão e etc.</p>
        </div>        
    </div>

    <div class="mt-5">
        <table class="table table-hover">                        
            <thead>
                <tr>
                    <th style="width: 5%;">#</th>
                    <th style="width: 20%;">Usuário</th>
                    <th style="width: 20%;">Senha</th>
                    <th style="width: 15%;">Admin Functions</th>
                </tr>
            </thead>
            <tbody>
                {{#each select}}
                <tr>
                    <td>{{id}}</td>                        
                    <td>{{user}}</td>
                    <td>{{pass}}</td>
                    <td>
                        <a href="/admin/delete/{{id}}"><button class="btn btn-danger mt-1">Deletar</button></a>
                        <a href="/admin/update/{{id}}"><button class="btn btn-warning mt-1">Atualizar</button></a>
                    </td>                       
                </tr> 
                {{/each}}                   
            </tbody>
        </table>
    </div>
</div>
    
<div class="mt-3"></div>